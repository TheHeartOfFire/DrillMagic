<UserControl
    x:Class="DrillMagic.Windows.Controls.DrillGridView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DrillMagic.Windows.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d">

    <Grid>
        <canvas:CanvasControl x:Name="Canvas" CreateResources="Canvas_CreateResources" Draw="Canvas_Draw" ClearColor="Transparent"/>
        <local:ColorSummaryLegend x:Name="ColorSummaryLegend"
                                  Summary="{x:Bind Grid.ColorSummary, Mode=OneWay}"
                                  HorizontalAlignment="Left"
                                  VerticalAlignment="Bottom"
                                  Margin="20"
                                  MaxWidth="350"
                                  MaxHeight="400"
                                  Visibility="{x:Bind Grid, Converter={StaticResource ObjectToVisibilityConverter}, Mode=OneWay}"/>
    </Grid>
</UserControl>